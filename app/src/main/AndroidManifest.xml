<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unifor.cct.gestaofinanceira">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.gestaofinanceira">
        <activity android:name="br.unifor.cct.gestaofinanceira.activity.LancamentoFormActivity"></activity>
        <activity
            android:name="br.unifor.cct.gestaofinanceira.activity.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="br.unifor.cct.gestaofinanceira.activity.RegisterActivity" />
        <activity android:name="br.unifor.cct.gestaofinanceira.activity.LoginActivity" />
        <activity android:name="br.unifor.cct.gestaofinanceira.activity.MainActivity" />
    </application>

</manifest>